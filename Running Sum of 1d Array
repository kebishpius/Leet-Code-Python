class Solution(object):
    def runningSum(self, nums):
        sums = 0
        storesums = []
        for i in range(len(nums)):
            sums = nums[i] + sums
            storesums.append(sums)
        return storesums
